#declaração implícita numérico
delta = 8
class(delta)

#declarar como inteiro
delta = 8L
class(delta)

#outros tipos
logico = TRUE
logico = F
class(logico)
caractere = "Texto"
class(caractere)
caractere = 'Texto'
class(caractere)

#Operações matemáticas
c = 2
d = 4
(c + d) * d
e = (c + d) * d
e

#Teste lógico
a = 1
b = 2
a < b
a == b

#Raiz Quadrada
sqrt(2500)
