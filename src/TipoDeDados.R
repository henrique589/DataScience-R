#declaração implícita numérico
delta = 8
class(delta)

#declarar como inteiro
delta = 8L
class(delta)

#outros tipos
logico = TRUE
logico = F
class(logico)
caractere = "Texto"
class(caractere)
caractere = 'Texto'
class(caractere)

#Operações matemáticas
